import styled from "styled-components";
import COLORS from "../../data/colors";
const Cont = styled.div`

`;
const Listing = ({address, created_at, icon, tags,  }) => {
  return (
    <Cont colors = {COLORS}>

    </Cont>
      )
}

export default Listing